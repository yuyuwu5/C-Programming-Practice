#include<stdio.h>
int main()
{
    int num[100],h=0;
    while(scanf("%d",&num[h])!=EOF)
        h++;
    int start, how_long=0;
    for(int i = 0; i < h; i++){
        for(int j = 0; j < h; j++){
            
        }
    }
}